spring:
  application:
    name: StockServer-Test
  flyway:
    enable: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
stock-server:
  tiingo:
    api_key: dummy
  alpha_vantage:
    api_key: dummy
